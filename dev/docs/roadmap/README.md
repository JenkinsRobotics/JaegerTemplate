# dev/docs/roadmap/

What's planned but not yet built. Every claim in here is implicitly
`(planned)` — see `CONVENTIONS.md` → "No spec ahead of code." When
something ships, move its description to `../reality/STATUS.md` (or a
`../history/` write-up) and delete it from here — don't let the same
fact live in two places.

Empty until this repo has a real roadmap.
